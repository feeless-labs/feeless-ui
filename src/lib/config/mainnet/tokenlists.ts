import { TokenListURLMap } from '@/types/TokenList';

const tokenlists: TokenListURLMap = {
  Balancer: {
    Allowlisted:
      'https://raw.githubusercontent.com/feeless-labs/feeless-deployments/master/feeless.testnet.tokenlist.json',
  },
  External: [],
};

export default tokenlists;
